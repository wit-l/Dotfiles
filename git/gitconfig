[core]
  pager = delta
[interactive]
  diffFilter = delta --color-only
[delta]
    features = line-numbers
    navigate = true  # use n and N to move between diff sections
[user]
	name = wit-l
	email = witty.ysl@gmail.com
[init]
	defaultBranch = main
[pull]
	rebase = true
[advice]
	mergeConflict = false
[credential]
	helper = cache --timeout 21600
	helper = oauth
